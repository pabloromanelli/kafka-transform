{
  "rules": [
    {
      "query": "some query",
      "template": {
        "origin": "AAA111",
        "name": "{{value}}"
      }
    },
    {
      "query": "some query",
      "template": {
        "origin": "ABC123",
        "name": "{{value}}!!!"
      }
    }
  ]
}